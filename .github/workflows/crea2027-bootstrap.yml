name: Preparar proyecto CREA 2027

on:
  push:
    branches: [main]
    paths: [crea2027-bootstrap.part-06]

permissions:
  contents: write

jobs:
  bootstrap:
    if: github.actor == github.repository_owner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Incorporar el proyecto validado
        run: |
          cat crea2027-bootstrap.part-* | base64 --decode > /tmp/crea2027-bootstrap.zip
          unzip -o /tmp/crea2027-bootstrap.zip
          rm crea2027-bootstrap.part-*
          rm .github/workflows/crea2027-bootstrap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: publica sitio CREA Design Studio 2.0"
          git push
